import { FiltrarListaPipe } from './filtrar-lista.pipe';

describe('FiltrarListaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarListaPipe();
    expect(pipe).toBeTruthy();
  });
});
